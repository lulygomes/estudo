type Perfil {
    id: Int
    nome: String
}

enum UsusarioStatus {
    ATIVO
    INATIVO
    BLOQUEADO
}


type Usuario {
    id: ID
    nome: String!
    email: String!
    idade: Int
    salario: Float
    vip: Boolean
    perfil: Perfil
    status: UsusarioStatus
}
